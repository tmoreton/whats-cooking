AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  Authenticated HTTP API + Lambda proxy for the "What's Cooking?" app. The proxy
  SigV4-signs to the AgentCore runtime. The public route uses AWS_IAM auth; the
  app obtains short-lived guest credentials from a Cognito Identity Pool and
  SigV4-signs its requests — so no long-lived secret ships in the app.

Parameters:
  AgentRuntimeArn:
    Type: String
    Description: ARN of the deployed AgentCore runtime.
    Default: arn:aws:bedrock-agentcore:us-east-1:253170388727:runtime/whats_cooking-iik9UNDwCW
  AgentQualifier:
    Type: String
    Description: Runtime endpoint qualifier.
    Default: DEFAULT

Globals:
  Function:
    Runtime: python3.12
    Architectures: [arm64]
    MemorySize: 1024
    # HTTP API caps integration timeout at 29s; vision calls observed at ~9-18s.
    Timeout: 29
    Tracing: Active
    Environment:
      Variables:
        POWERTOOLS_LOG_LEVEL: INFO

Resources:
  # ---- API ----
  ApiAccessLogs:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/http-api/${AWS::StackName}-access'
      RetentionInDays: 14

  ProxyApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      CorsConfiguration:
        AllowOrigins: ['*']
        AllowHeaders: ['content-type', 'authorization', 'x-amz-date', 'x-amz-security-token']
        AllowMethods: ['POST', 'OPTIONS']
      # Cap throughput so a runaway client can't run up unbounded Bedrock cost.
      DefaultRouteSettings:
        ThrottlingRateLimit: 5
        ThrottlingBurstLimit: 10
      Auth:
        # AWS_IAM: callers must SigV4-sign with (guest) credentials.
        EnableIamAuthorizer: true
        DefaultAuthorizer: AWS_IAM
      AccessLogSettings:
        DestinationArn: !GetAtt ApiAccessLogs.Arn
        Format: '{"requestId":"$context.requestId","ip":"$context.identity.sourceIp","routeKey":"$context.routeKey","status":"$context.status","protocol":"$context.protocol","responseLatency":"$context.responseLatency","authError":"$context.authorizer.error"}'

  # ---- Auth: Cognito Identity Pool with guest (unauthenticated) access ----
  IdentityPool:
    Type: AWS::Cognito::IdentityPool
    Properties:
      IdentityPoolName: !Sub '${AWS::StackName}-guests'
      AllowUnauthenticatedIdentities: true

  GuestRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${AWS::StackName}-guest-role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Federated: cognito-identity.amazonaws.com
            Action: sts:AssumeRoleWithWebIdentity
            Condition:
              StringEquals:
                'cognito-identity.amazonaws.com:aud': !Ref IdentityPool
              'ForAnyValue:StringLike':
                'cognito-identity.amazonaws.com:amr': unauthenticated
      Policies:
        - PolicyName: invoke-recipe-api
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: execute-api:Invoke
                # Least privilege: only the POST /invocations route of this API.
                Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${ProxyApi}/*/POST/invocations'

  IdentityPoolRoleAttachment:
    Type: AWS::Cognito::IdentityPoolRoleAttachment
    Properties:
      IdentityPoolId: !Ref IdentityPool
      Roles:
        unauthenticated: !GetAtt GuestRole.Arn

  # ---- Lambda proxy ----
  RecipeProxyFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${AWS::StackName}-recipe-proxy'
      CodeUri: src/
      Handler: app.handler
      Environment:
        Variables:
          AGENT_RUNTIME_ARN: !Ref AgentRuntimeArn
          AGENT_QUALIFIER: !Ref AgentQualifier
      Policies:
        - Statement:
            - Effect: Allow
              Action: bedrock-agentcore:InvokeAgentRuntime
              Resource:
                - !Ref AgentRuntimeArn
                - !Sub '${AgentRuntimeArn}/*'
      Events:
        Invoke:
          Type: HttpApi
          Properties:
            ApiId: !Ref ProxyApi
            Path: /invocations
            Method: POST
            Auth:
              Authorizer: AWS_IAM

  RecipeProxyLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/lambda/${AWS::StackName}-recipe-proxy'
      RetentionInDays: 14

Outputs:
  ApiUrl:
    Description: EXPO_PUBLIC_API_URL
    Value: !Sub 'https://${ProxyApi}.execute-api.${AWS::Region}.amazonaws.com/invocations'
  IdentityPoolId:
    Description: EXPO_PUBLIC_IDENTITY_POOL_ID
    Value: !Ref IdentityPool
  AwsRegion:
    Description: EXPO_PUBLIC_AWS_REGION
    Value: !Ref AWS::Region
